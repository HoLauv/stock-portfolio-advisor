# Changelog

本文件记录模型的**规则与数据质量变更**。评分结果带 `model_version`，跨版本快照不直接计算分差。

## 2.3.0

**新增股息子项（红利类标的的持有理由此前无法被计入）**

- 问题：模型完全没有股息输入。宁沪高速/京沪高铁/中国核电这类标的，其持有理由（稳定现金分红）
  在 V 和 Q 里都无处体现，估值只能靠 PE/PB 分位与安全边际判断，红利逻辑等于零信号。
- 新增两项派生指标，均**不接受调用方直接输入**，一律由原始量重算：
  - `dividend_spread_pct = dividend_yield_pct − market.risk_free_rate_pct`（股息率 − 10年期国债），
    进 **V**，权重20；曲线 `-1→0 / 0→50 / 1→80 / 2.5→100` 个百分点。启用时 V 变为 50/15/15/20。
  - `dividend_coverage = net_profit_ttm / cash_dividend_ttm`（盈利覆盖派息倍数），
    进 **Q**，权重10；曲线 `0.8→0 / 1.2→45 / 1.7→75 / 2.5→100` 倍。
- **分工刻意不重叠**：利差衡量现金回报吸引力（估值），覆盖倍数衡量派息可持续性（质量）。
- **整块启用**：两项必须同时可算；缺有效无风险利率或现金分红则整块不进分母，
  因此**没有股息数据的标的评分与 2.2.0 完全一致**，不会因"没查分红"被变相扣分。
- **权重总和恒为100**：追加 Q 子项时其余项按 0.9 等比压缩，避免分母变110导致原有各项被动降权。
- 结果新增 `dividend` 字段（含 `spread_score` / `coverage_score`），报告单独展示，
  便于核对持有理由是否被正确计入。
- **已知偏置**：股息子项是可选启用，同一只高Q标的补上股息数据后，若其覆盖分低于原有各项，
  Q 会下降——评估了更多信息，结果自然可能变差。缓解办法是对红利类档位**始终取股息数据**，
  让横向比较建立在同一口径上；报告与 `notes` 会标注本次是否启用。

**行业景气度改由申万行业财报派生（替代对所有标的等额 50 分）**

- 问题：`industry_boom` 原来靠手填或按期望分 50 插补，所有标的拿到同一个数，"不含个股信息"。
- 修复：新增输入 `sector_np_yoy_pct`（行业TTM净利同比，必填）与 `sector_rev_yoy_pct`（行业TTM营收同比，可选），
  引擎按 **净利70% + 营收30%** 派生 `industry_boom`（只有净利时单口径）。
  净利曲线 `-20→0/0→35/10→60/25→85/50→100`，营收曲线 `-10→0/0→40/10→70/25→100`。
- **派生覆盖手填**：有行业数据时派生值优先；行业数据缺失或过不了证据门槛才退回手填；
  两者都没有才按期望分 50 插补。插补仍是最后兜底，保留 2.2.0"第三方数据缺失不惩罚公司"的原则。
- 派生成功时 `data_quality.<维度>.imputed` 不再出现 `industry_boom`，可据此区分真实行业数据与插补。

**市场温度打通：三个指标改为可自算**

- 问题：`volume_ratio_5_250` 等四个指标全靠外部提供，缺一个就整个温度分输出 null，
  实际上多数都能从行情序列算出来。
- 修复：新增派生层，显式给值优先，缺失才自算——

  | 派生指标 | 原始序列 | 最少样本 | 算法 |
  | --- | --- | --- | --- |
  | `hs300_ma250_dev` | `hs300_close_series` | 250 | `(最新/近250日均值-1)×100` |
  | `volume_ratio_5_250` | `turnover_series`（全市场成交额近似量能） | 250 | `近5日均值/近250日均值` |
  | `up_ratio_20d` | `advance_ratio_series` | 20 | `近20日均值` |

- 序列需自带 `indicator_meta.<序列名>`，派生值继承来源 meta 后照常过证据门槛，不走后门。
  序列过短、含非有限值、成交额≤0、上涨占比越界时整体作废并保持缺失。
- 结果新增 `market.derived` 列出本次哪些是自算的，`notes` 同步写明。
  `broken_net_ratio` 无法派生，仍需外部提供。
- **`up_ratio_20d` 的窗口语义明确为"近20个交易日上涨家数占比的均值"**，不是当日值。

**同时修掉数据手册里的命令错误**

- `quote` 与顶层 `flow` 在 `westock-data-skillhub@1.0.5` 中**不存在**：
  现价改用 `kline --limit 1`，资金改用 `fund flow`，北向改用 `fund north-holding`。
- 分红命令缺子命令：正确写法是 `dividend list`。
- 板块搜索不是 `sector search`，而是 `search <关键词> --type sector`。

**其它**

- 单元测试 72 个（+18），覆盖股息启用/缺半不启用/派生值不可注入/权重重配、
  景气度派生与证据门槛与兜底、温度自算与输入优先与序列校验。
- 示例新增「红利样本」股票展示股息子项；两只样本补行业财报字段；
  市场块改用原始序列展示自算路径。
- 跨版本分差一律 `comparable=False`，已有用户的评分基线会重建。

## 2.2.0

**结构性缺失项按总体期望分插补（修复 G 维度被系统性低估）**

- 问题：`score_dimension` 用 `earned / Σ适用项权重 × 100`，缺失项直接计 0。G 的 `forecast`(20) + `industry_boom`(10) 在很多标的上没有数据，于是这些标的的 G 上限被硬压到 70 分，且覆盖率恰好停在 70% 门槛上——再缺任一项就直接 NR。这是把"没有机构覆盖"误判成"成长性差"。
- 修复：新增白名单常量 `IMPUTE = {'forecast': 60, 'industry_boom': 50}`。命中白名单的缺失项按**总体期望分**计价（`forecast` 的期望分 = 0.25×beat100 + 0.40×inline70 + 0.15×flat40 + 0.20×miss0 ≈ 60），并在 `data_quality.<维度>.imputed` 与 `notes` 中留痕。
- **白名单边界**：只收"第三方来源缺失、公司自身并不掌握"的前瞻/行业项。公司自己披露的财务/资金/技术项（ROE、毛利率、负债率、资金流、波动率等）**一律不插补**，缺失仍贡献 0——否则漏报坏数据反而加分。这是有意的不对称。
- **覆盖率不变**：`coverage` 只统计真实数据权重，插补不抬高覆盖率，`MIN_COVERAGE=70` 的 NR 门槛完全不受影响。
- 影响量化：`forecast` + `industry_boom` 同时缺失时 G 恰好 +17.0（12 + 5），价值分位移按画像为 default +5.30 / growth +7.90 / value +2.40 / cyclical +4.00 / bank +2.60。数据齐全的标的位移为 **0**，修改是精准作用域的。
- **已知偏置（已接受）**：无机构覆盖的标的（插补后 G=76.9）会高于有覆盖但预期落空（`forecast='miss'`，G=67.9）的标的。只要插补值 > 0，这个倒挂就无法消除，是本方案的固有代价；缓解手段是调 `IMPUTE` 常量，或横向比较时参考 `data_quality.*.imputed`。
- 示例新增 `no_coverage` 样本展示该行为；单元测试 54 个（+5）。跨版本分差一律 `comparable=False`，**已有用户的评分基线会重建**。

**同时修复配置填充的顺序依赖缺陷**

- 问题：`allocate()` 按代码排序**逐只顺序**累加权重，而分组剩余额度读取的是已被前面标的改过的状态。同一行业内 3 只以上标的时，代码排序靠前的两只先占满 30% 行业额度，总分最高的反而只剩残值。示例中曾出现 87.5 分标的只分到 3%、86.0 分标的却分到 15%。
- 修复：抽出 `allocate_budget()`，每轮基于**同一份权重快照**计算额度、一次性提交增量；分组额度不足时按 merit 比例等比压缩该组额度，且压缩发生在单票上限之前（先截断再压缩会退化成平均分配）。分配结果现在只由评分决定，与代码命名无关。
- 新增回归测试 `test_allocation_ignores_code_order_within_group`：同名标的换代码顺序后分配结果完全一致，且严格按评分递减。

**打包白名单修正（发布阻塞项）**

- `assets/icon.png` 不再进包：SkillHub 服务端以「不允许的文件类型: assets/icon.png」拒绝整个上传。技能图标本就通过平台托管的 `iconUrl` 字段下发，不属于随包文件（线上已发布的 2.0.0 包内同样没有该文件）。仓库仍保留该图供 GitHub 展示。
- 白名单改为「必需 + 可选」两段：SKILL.md、`scripts/`、`references/`、`config/` 缺失直接报错并列出文件名；`assets/` 下文件缺失只跳过。此前 `assets/icon.prompt.md` 缺失会抛出裸的 `FileNotFoundError`，难以定位。

## 2.1.0

**规则与数据质量升级（未完成历史收益校准）**

- 拆分价值分与交易环境分：价值分 `(Q×wQ+G×wG+V×wV)/(wQ+wG+wV)` 按画像路由，交易环境分 `(15F+10T+10N)/35` 独立展示，不再混入价值判断
- 分行业 Q 指标：bank / broker / insurance 各有独立口径（不良率与资本缓冲 / 风险覆盖率 / 偿付能力），不再共用一套
- 有效覆盖率门槛：单维度有效覆盖率 <70% 时该维度为 `null`，不会把少量可得数据放大成高分
- 缺失项保留在分母中、贡献 0
- 资料不足输出 `NR`（暂不评级），总分与仓位上限均为 `null`；已知 ST / 退市 / 立案仍强制 `D`、上限 0
- 估值改为分行业主模型 + bear/base/bull 三情景公允价值，不做技术位兜底
- 配置约束收紧：单票 ≤15%、单一行业 ≤30%、产业链分组 ≤30%、现金 ≥ max(5%, 现金需求)
- 大盘温度改为均线 / 成交额 / 涨跌广度 / 破净比例 4 项（权重 30:20:20:15），取消北向 20 日净流入依赖，缺项不输出温度
- 权重版本化（`weights_version`），结果带 `model_version`
- 新增回测模拟器（`backtest.py`）与权重校准（`calibrate_weights.py`）
- 单元测试 49 个（验证公式、缺失值、行业路由、风险、仓位约束、报告兼容性；**非历史收益回测**）

## 1.0.0

- 首个版本：六维评分（Q 质量 25 / G 成长 20 / V 估值 20 / F 资金 15 / T 技术 10 / N 新闻 10）合成总分
- 目标价三法取中位数、止损位、大盘温度、配置硬约束
- HTML 报告（内联 SVG，离线可用）与评分快照
- v1 的问题留档见 `references/valuation-review-2026-09-05.md`，**不覆盖当前 v2 规范**
